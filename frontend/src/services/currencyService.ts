export interface CurrencyConversion {
  naira_amount: number;
  sui_amount: number;
  exchange_rate: number;
  timestamp: string;
}

export interface ExchangeRate {
  sui_to_ngn: number;
  ngn_to_sui: number;
  timestamp: string;
  cached: boolean;
}
export interface CurrencyConversion {
  naira_amount: number;
  sui_amount: number;
  exchange_rate: number;
  timestamp: string;
}

export interface ExchangeRate {
  sui_to_ngn: number;
  ngn_to_sui: number;
  timestamp: string;
  cached: boolean;
}
